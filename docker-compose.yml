version: '3'
services:
  docker_flask_app:
    image: docker_flask_app/v1
    container_name: docker_flask_container
    build:
      context: .
      args:
        - VERSION=v2.0
    ports:
      - 8080:5000
    environment:
      - ENV_VAR=${ENVIRONMENT}
    env_file: .env
    restart: unless-stopped
